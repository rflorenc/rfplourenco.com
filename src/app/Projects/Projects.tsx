import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Projects: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="4xl">Projects Page Title</Title>
  </PageSection>
)

export { Projects };
